openapi: 3.0.0
servers:
  - description: AthletX API
    url: http://localhost:<port>/api/v1
info:
  description: This is the API of the AthletX Server
  version: 1.1.0
  title: AthletX API
  contact:
    email: info@athletx.com
  license:
    name: MIT License
    url: 'https://opensource.org/license/mit/'

tags:
  - name: Auth API
    description: This is the available API to authenticate users.

paths:
  /auth:
    post:
      tags:
        - Auth API
      summary: auth
      operationId: authUser
      description: Auth user
      responses:
        '200':
          description: authenticated